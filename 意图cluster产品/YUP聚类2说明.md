# YUP聚类2（KYC导向）说明

## 问题1：聚类2是如何出现的？

### 聚类2的来源
用户 `011c40cc5105419293c6a52635546b4a` 的第一个片段（segment_index: 0）被归类到聚类2（KYC导向）。

### 片段0的特征
- **事件名称**: `show_live_face_verification_page`（显示人脸验证页面）
- **时长**: 0.0分钟（瞬时事件）
- **记录数**: 1条
- **意图强度**: 0.5

### 为什么被归类到聚类2？
聚类算法根据以下特征将其归类到聚类2（KYC导向）：

1. **KYC相关特征**:
   - `kyc_started: 1`（已开始KYC）
   - `kyc_event_count: 1`（有1个KYC事件）
   - `business_scenario: 1`（业务场景为"KYC进行中"）

2. **用户状态特征**:
   - `first_order_completed: 1`（已完成首单）
   - `post_first_order: 1`（首单后行为）

3. **行为特征**:
   - `navigation_related_events: 1`（导航相关事件）
   - `event_count: 1`（总事件数）

### 聚类2的标签
- **full_label**: "首单后活跃·中紧迫·KYC导向"
- **main_activity**: "KYC导向"
- **behavior**: "首单后活跃"
- **kyc_status**: "已开始"
- **transaction_status**: "进行中"

## 问题2：为什么用户轨迹里没有聚类2？

### 数据确认
✅ **用户轨迹数据中确实包含聚类2**：
- `cluster_ids: ['0', '2']`
- `unique_clusters: 2`
- 片段0显示为 `cluster_id=2, full_label=首单后活跃·中紧迫·KYC导向`

### 可能的原因
1. **前端显示问题**：
   - 聚类标签可能没有正确显示
   - 时长为0的片段可能在时间线上显示不明显

2. **数据同步问题**：
   - 浏览器可能缓存了旧数据
   - 需要刷新页面或清除缓存

### 验证方法
1. 检查浏览器控制台，查看 `userTrajectories` 数据
2. 检查用户轨迹卡片中的"聚类分布"部分，应该显示"聚类 0"和"聚类 2"
3. 检查时间线可视化，片段0应该显示为聚类2

## 聚类2的统计信息

- **片段数**: 1个片段
- **用户数**: 1位用户（`011c40cc5105419293c6a52635546b4a`）
- **平均时长**: 0.0分钟
- **平均记录数**: 1条
- **平均意图强度**: 0.5

## 建议

1. **刷新浏览器页面**，确保加载最新数据
2. **检查用户轨迹卡片**，确认"聚类分布"部分显示"聚类 0"和"聚类 2"
3. **检查时间线可视化**，片段0应该显示为聚类2（KYC导向）
4. 如果仍然看不到聚类2，请检查浏览器控制台是否有错误信息

