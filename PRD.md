# 用户意图聚类分析平台 - 产品需求文档 (PRD)

**版本**: v1.1  
**日期**: 2025-01-XX  
**作者**: Product Team  
**状态**: 正式版（已更新：基于意图变化的分段策略）

---

## 📋 目录

1. [产品概述](#1-产品概述)
2. [目标用户](#2-目标用户)
3. [核心功能](#3-核心功能)
4. [聚类算法设计](#4-聚类算法设计)
5. [前端可视化设计](#5-前端可视化设计)
6. [技术架构](#6-技术架构)
7. [数据模型](#7-数据模型)
8. [用户流程](#8-用户流程)
9. [性能要求](#9-性能要求)
10. [未来规划](#10-未来规划)

---

## 1. 产品概述

### 1.1 产品定位

用户意图聚类分析平台是一个面向独立站商家的智能数据分析工具，通过分析用户浏览行为数据，自动识别用户意图并进行聚类分析，帮助商家快速捕捉用户意图并制定相应的营销策略。

### 1.2 核心价值

- **智能识别用户意图**：基于用户行为数据自动识别购买阶段、产品偏好、价格敏感度等
- **自动聚类分析**：将相似意图的用户自动分组，形成可操作的细分群体
- **可视化展示**：通过直观的图表和仪表板展示分析结果
- **业务指导**：为每个用户群体提供针对性的营销策略建议

### 1.3 产品目标

- **短期目标**：帮助商家快速了解用户意图分布，优化产品推荐和营销策略
- **长期目标**：建立完整的用户意图分析体系，实现精准营销和个性化推荐

---

## 2. 目标用户

### 2.1 主要用户

- **独立站商家**：需要了解用户意图，优化营销策略
- **电商运营人员**：需要数据支持制定运营策略
- **数据分析师**：需要深入分析用户行为模式

### 2.2 用户需求

- 快速了解用户意图分布
- 识别高价值用户群体
- 获取针对性的营销建议
- 监控用户行为变化趋势

---

## 3. 核心功能

### 3.1 数据总览 (Overview)

#### 3.1.1 功能描述
提供整体数据概览，包括用户聚类分布、购买阶段分布、价格偏好分布、核心关注点等关键指标。

#### 3.1.2 功能点

1. **用户聚类分布**
   - 显示所有聚类的用户数量分布
   - 使用环形图（Doughnut Chart）展示
   - 支持点击查看详情

2. **购买阶段分布**
   - 展示用户处于不同购买阶段的比例
   - 阶段包括：浏览、对比、决策
   - 使用环形图展示

3. **价格偏好分布**
   - 展示用户的价格敏感度分布
   - 分类：预算导向、中端平衡、高端价值型
   - 使用柱状图展示

4. **核心关注点**
   - 展示用户最关心的产品特性
   - 使用水平柱状图展示
   - 支持排序和筛选

#### 3.1.3 交互要求
- 图表支持悬停显示详细数据
- 支持图表类型切换
- 支持数据导出

### 3.2 转化分析 (Conversion Analysis)

#### 3.2.1 功能描述
分析用户转化路径，识别转化漏斗和用户轨迹，实时追踪用户行为对转化的影响。

#### 3.2.2 功能点

1. **转化漏斗分析**
   - 展示用户在不同阶段的流失情况
   - 使用漏斗图展示
   - 显示转化率和流失率
   - 支持点击查看详情

2. **用户轨迹分析**
   - 展示用户的浏览路径
   - 使用流程图展示
   - 识别常见路径和异常路径
   - 支持节点展开查看详情

3. **转化路径可视化**
   - 展示用户从浏览到决策的完整路径
   - 使用时间线展示
   - 支持路径筛选和对比
   - 支持时间范围选择

4. **实时意图识别**
   - 实时分析用户浏览行为，识别用户意图
   - 追踪用户滚动、悬停、点击等行为
   - 基于行为数据实时识别购买阶段、产品偏好、价格敏感度等
   - 使用机器学习模型进行预测
   - 支持行为回放

5. **视频交互分析**
   - 追踪用户与产品视频的交互
   - 记录播放、暂停、观看时长等
   - 分析视频交互对转化指标的影响
   - 展示视频交互对转化阶段的影响
   - 展示参与度提升情况
   - 使用对比图表展示交互前后变化

#### 3.2.3 交互要求
- 漏斗图支持点击查看详情
- 流程图支持节点展开
- 时间线支持时间范围选择
- 实时更新意图数据
- 支持行为事件筛选
- 支持数据导出

### 3.3 用户分析 (User Analysis)

#### 3.3.1 功能描述
深入分析每个用户聚类的特征，生成详细的用户画像，包括用户规模、行为模式、意图特征、偏好分析等。

#### 3.3.2 功能点

1. **聚类列表展示**
   - 以卡片形式展示所有聚类
   - 每个卡片显示：聚类名称、用户数量、关键特征
   - 支持搜索和筛选
   - 聚类卡片支持展开/收起

2. **聚类详情**
   - 点击聚类卡片查看详细信息
   - 包括：
     - 用户规模统计
     - 浏览时长分布
     - 交互次数统计
     - 意图画像（核心兴趣、价格偏好、购买阶段）
     - 产品偏好分析
     - 行为模式分析
     - 时间模式分析
   - 详情页面支持返回列表

3. **聚类对比**
   - 支持选择多个聚类进行对比
   - 对比维度：用户规模、参与度、转化率等

4. **用户画像概览**
   - 显示聚类的核心特征标签
   - 使用词云或标签列表展示
   - 支持标签筛选

5. **特征雷达图**
   - 使用雷达图展示多维度特征
   - 维度包括：购买阶段、价格敏感度、产品偏好、关注点、核心需求
   - 支持多聚类对比
   - 支持交互式查看

6. **行为模式分析**
   - 展示用户的行为模式分布
   - 包括：高参与度、快速浏览、深度研究、多会话等
   - 使用饼图或柱状图展示

7. **时间模式分析**
   - 展示用户的活跃时段分布
   - 使用热力图展示
   - 支持按日期、时段筛选
   - 支持缩放和筛选

#### 3.3.3 交互要求
- 聚类卡片支持展开/收起
- 详情页面支持返回列表
- 雷达图支持交互式查看
- 热力图支持缩放和筛选
- 支持聚类数据导出
- 支持画像数据导出

---

## 4. 聚类算法设计

### 4.1 算法概述

采用基于行为意图的聚类算法，结合用户行为特征和意图特征进行聚类分析。

**核心创新：基于意图变化的分段策略**
- 不再机械地按固定时间间隔（如10分钟）分段
- 智能识别用户意图的真实变化，仅在意图变化时创建新片段
- 一个用户可以拥有多个意图片段，反映用户在不同时间段的意图变化
- 时间间隔仅作为辅助判断机制，确保长时间不活跃的用户行为被正确分段

### 4.2 数据预处理

#### 4.2.1 意图分段（核心改进）

- **目标**：将连续的用户行为数据分割成有意义的意图片段，一个用户可以拥有多个不同意图的片段
- **核心策略**：基于意图变化进行分段，而非机械地按时间间隔分段
- **方法**：
  - **主要策略**：当用户意图真正发生变化时才进行意图切片
    - 提取每个行为的意图特征（购买阶段、产品偏好、关注点、核心需求、价格敏感度、意图强度）
    - 计算相邻行为之间的意图变化程度
    - 当意图变化超过阈值（默认0.3）时，创建新的意图片段
  - **辅助策略**：时间间隔作为兜底机制
    - 时间间隔阈值（默认10分钟）：当时间间隔较长且无法提取意图时，也进行分段
    - 不活跃阈值（默认15分钟）：超过此时间间隔强制分段，避免误判
  - 每个意图片段作为一个独立的分析单元

- **意图变化计算**：
  - 使用加权算法计算意图变化程度（0-1）
  - 特征权重：
    - 购买阶段变化：30%（最高权重）
    - 产品偏好变化：25%
    - 核心需求变化：20%
    - 关注点变化：15%
    - 价格敏感度变化：10%
    - 意图强度变化超过0.2：额外+10%
  - 当总变化度 ≥ 阈值时，认为意图发生显著变化

- **优势**：
  - 更准确地反映用户意图的真实变化
  - 允许一个用户在不同时间段拥有不同的意图
  - 避免因时间间隔导致的误分段
  - 提高聚类结果的业务价值

#### 4.2.2 特征提取

**行为特征**：
- `record_count`: 交互次数（意图片段内的行为事件数）
- `duration_minutes`: 浏览时长（意图片段持续时间，分钟）
- `intent_score`: 意图强度（0-1，基于行为数据计算）

**意图特征**（从意图识别结果中提取）：

所有意图特征都从用户行为记录中的 `output` 字段（JSON格式的意图识别结果）中提取：

1. **`purchase_stage`: 购买阶段** (0=浏览, 1=对比, 2=决策)
   - **提取路径**: `intent.purchase_signals.stage`
   - **提取逻辑**:
     ```python
     stage = intent.get('purchase_signals', {}).get('stage', 'browsing')
     if 'deciding' in str(stage).lower():
         purchase_stage = 2  # 决策阶段
     elif 'comparing' in str(stage).lower():
         purchase_stage = 1  # 对比阶段
     else:
         purchase_stage = 0  # 浏览阶段（默认）
     ```

2. **`product_preference`: 产品偏好** (0-5，对应不同产品型号)
   - **提取路径**: `intent.core_interests` + `intent.product_focus.key_attributes`
   - **提取逻辑**:
     ```python
     # 合并核心兴趣和产品属性文本
     all_text = ' '.join([core_interests + product_focus_attrs]).lower()
     
     # 统计各产品关键词出现频率
     product_counts = {
         'z6': all_text.count('z6') + all_text.count('mems'),
         'a1': all_text.count('a1') + all_text.count('ai anti-snore'),
         'f1': all_text.count('f1') + all_text.count('ergonomic'),
         'h02': all_text.count('h02') + all_text.count('neck'),
         'g1': all_text.count('g1') + all_text.count('mattress')
     }
     
     # 选择频率最高的产品
     max_product = max(product_counts.items(), key=lambda x: x[1])
     if max_product[1] > 0:
         product_map = {'z6': 0, 'a1': 1, 'f1': 2, 'h02': 3, 'g1': 4}
         product_preference = product_map.get(max_product[0], 5)
     else:
         product_preference = 5  # 无明确偏好
     ```

3. **`concern_focus`: 关注点** (0-4，对应不同关注维度)
   - **提取路径**: `intent.purchase_signals.concerns`
   - **提取逻辑**:
     ```python
     concerns = purchase_signals.get('concerns', [])
     concerns_text = ' '.join([str(x).lower() for x in concerns])
     
     if 'price' in concerns_text or 'cost' in concerns_text or 'discount' in concerns_text:
         concern_focus = 1  # 价格导向
     elif 'comfort' in concerns_text or 'comfortable' in concerns_text:
         concern_focus = 2  # 舒适度导向
     elif 'effectiveness' in concerns_text or 'effective' in concerns_text or 'work' in concerns_text:
         concern_focus = 3  # 有效性导向
     elif 'feature' in concerns_text or 'function' in concerns_text or 'technology' in concerns_text:
         concern_focus = 0  # 功能导向
     else:
         concern_focus = 4  # 综合关注（默认）
     ```

4. **`core_need`: 核心需求** (0-3，对应不同需求类型)
   - **提取路径**: `intent.product_focus.main_appeal`
   - **提取逻辑**:
     ```python
     main_appeal = product_focus.get('main_appeal', '')
     main_appeal_lower = str(main_appeal).lower()
     
     if 'snore' in main_appeal_lower or 'snoring' in main_appeal_lower:
         core_need = 0  # 止鼾需求
     elif 'neck' in main_appeal_lower or 'pain' in main_appeal_lower:
         core_need = 1  # 颈部疼痛缓解
     elif 'sleep' in main_appeal_lower and 'quality' in main_appeal_lower:
         core_need = 2  # 睡眠质量改善
     else:
         core_need = 3  # 综合需求（默认）
     ```

5. **`price_sensitivity`: 价格敏感度** (0=预算导向, 1=中端, 2=高端)
   - **提取路径**: `intent.product_focus.price_range`
   - **提取逻辑**:
     ```python
     price_range = product_focus.get('price_range', 'premium')
     price_range_lower = str(price_range).lower()
     
     if 'budget' in price_range_lower or 'economy' in price_range_lower:
         price_sensitivity = 0  # 预算导向
     elif 'mid' in price_range_lower or 'medium' in price_range_lower:
         price_sensitivity = 1  # 中端平衡
     else:
         price_sensitivity = 2  # 高端价值型（默认）
     ```

**数据源格式示例**：
```json
{
  "user_id": "user_123",
  "timestamp": "2025-01-01T10:00:00Z",
  "output": "{\"intent_score\": 0.75, \"intent\": {\"purchase_signals\": {\"stage\": \"considering\", \"concerns\": [\"功能\", \"有效性\"]}, \"product_focus\": {\"price_range\": \"premium\", \"main_appeal\": \"止鼾\", \"key_attributes\": [\"Comfort\", \"Sleep\"]}, \"core_interests\": [\"止鼾\", \"睡眠质量\"]}}"
}
```

**特征提取流程**：
1. 解析 `output` 字段中的JSON字符串
2. 提取 `intent` 对象及其嵌套字段
3. 根据预定义的规则和关键词匹配，将文本信息映射为数值特征
4. 如果某个字段缺失或无法匹配，使用默认值

#### 4.2.3 特征工程

1. **对数变换**
   - 对长尾分布的特征进行对数变换
   - `record_count_log = log(record_count + 1)`
   - `duration_minutes_log = log(duration_minutes + 0.001)`

2. **标准化**
   - 使用 StandardScaler 对所有特征进行标准化
   - 确保不同量纲的特征在相同尺度上

### 4.3 聚类算法

#### 4.3.1 算法选择

**K-Means 聚类算法**

- **原因**：
  - 适合处理数值型特征
  - 计算效率高，适合大规模数据
  - 结果可解释性强

- **参数设置**：
  - `n_clusters`: 聚类数量（默认15，可通过肘部法则优化）
  - `random_state`: 随机种子（42，确保结果可复现）
  - `n_init`: 初始化次数（20，提高稳定性）

#### 4.3.2 聚类数量优化

**肘部法则 (Elbow Method)**
- 计算不同聚类数量下的SSE（误差平方和）
- 选择SSE下降速度明显变缓的点作为最优聚类数

**轮廓系数 (Silhouette Score)**
- 评估聚类质量
- 取值范围：[-1, 1]，越接近1表示聚类效果越好

### 4.4 业务驱动聚类

#### 4.4.1 业务特征提取

从意图识别结果中提取业务相关的特征：

1. **购买阶段识别**
   ```python
   if 'deciding' in stage: purchase_stage = 2
   elif 'comparing' in stage: purchase_stage = 1
   else: purchase_stage = 0
   ```

2. **价格敏感度识别**
   ```python
   if 'budget' or 'economy' in price_range: price_sensitivity = 0
   elif 'mid' or 'medium' in price_range: price_sensitivity = 1
   else: price_sensitivity = 2
   ```

3. **产品偏好识别**
   - 统计不同产品关键词的出现频率
   - 选择频率最高的产品作为偏好
   - 产品映射：Z6(0), A1(1), F1(2), H02(3), G1(4), 无偏好(5)

4. **关注点识别**
   - 从 concerns 字段提取关注点
   - 映射到预定义的关注维度

5. **核心需求识别**
   - 从核心兴趣和产品属性中提取
   - 映射到预定义的需求类型

#### 4.4.2 特征组合

最终特征向量包括：
- `intent_score`: 意图强度
- `purchase_stage`: 购买阶段
- `product_preference`: 产品偏好
- `concern_focus`: 关注点
- `core_need`: 核心需求
- `price_sensitivity`: 价格敏感度（0=预算导向, 1=中端, 2=高端）
- `record_count_log`: 交互次数（对数变换）
- `duration_minutes_log`: 浏览时长（对数变换）

**特征选择代码**：
```python
feature_cols = [
    'record_count',          # 交互次数
    'duration_minutes',      # 浏览时长
    'intent_score',          # 意图强度
    'purchase_stage',        # 购买阶段
    'product_preference',    # 产品偏好
    'concern_focus',         # 关注点
    'core_need',             # 核心需求
    'price_sensitivity'      # 价格敏感度
]
```

**特征向量最终包含8个特征**（经过对数变换和标准化后）：
1. `intent_score` - 意图强度
2. `purchase_stage` - 购买阶段
3. `product_preference` - 产品偏好
4. `concern_focus` - 关注点
5. `core_need` - 核心需求
6. `price_sensitivity` - 价格敏感度
7. `record_count_log` - 交互次数（对数变换）
8. `duration_minutes_log` - 浏览时长（对数变换）

### 4.5 聚类结果分析

#### 4.5.1 聚类统计

为每个聚类计算：
- 用户数量（独立用户数、意图片段数）
- 平均浏览时长
- 平均交互次数
- 意图强度分布
- 各特征的平均值
- 意图变化频率（反映用户意图的稳定性）

#### 4.5.2 聚类命名

基于聚类特征自动生成描述性名称：
- 提取关键特征（购买阶段、产品偏好、价格敏感度等）
- 组合生成聚类名称
- 例如："高意图·Z6偏好·决策阶段"

#### 4.5.3 聚类质量评估

- **轮廓系数**：评估聚类紧密度和分离度
- **类内距离**：评估聚类内部一致性
- **类间距离**：评估聚类之间的差异性

---

## 5. 前端可视化设计

### 5.1 技术栈

- **HTML5/CSS3**: 页面结构和样式
- **JavaScript (ES6+)**: 交互逻辑
- **Chart.js**: 图表库
- **Canvas API**: 自定义图表（热力图、流程图等）

### 5.2 页面结构

#### 5.2.1 布局设计

```
┌─────────────────────────────────────────┐
│            Header (导航栏)              │
├─────────────────────────────────────────┤
│                                         │
│         Main Content Area               │
│  ┌──────────┐  ┌──────────────────┐   │
│  │ Sidebar  │  │   Content Area   │   │
│  │ (可选)   │  │                  │   │
│  └──────────┘  └──────────────────┘   │
│                                         │
├─────────────────────────────────────────┤
│            Footer                        │
└─────────────────────────────────────────┘
```

#### 5.2.2 响应式设计

- **桌面端**：侧边栏 + 主内容区
- **平板端**：可折叠侧边栏
- **移动端**：底部导航栏

### 5.3 图表类型

#### 5.3.1 数据总览图表

1. **用户聚类分布 - 环形图 (Doughnut Chart)**
   - 展示各聚类的用户数量占比
   - 支持点击查看详情
   - 颜色编码区分不同聚类

2. **购买阶段分布 - 环形图**
   - 展示用户处于不同购买阶段的比例
   - 使用不同颜色区分阶段

3. **价格偏好分布 - 柱状图 (Bar Chart)**
   - 展示价格偏好类型的分布
   - 支持排序

4. **核心关注点 - 水平柱状图 (Horizontal Bar Chart)**
   - 展示用户关注点的排名
   - 支持筛选和排序

#### 5.3.2 用户画像图表

1. **特征雷达图 (Radar Chart)**
   - 展示多维度特征
   - 支持多聚类对比
   - 使用不同颜色区分聚类

2. **行为模式分布 - 饼图 (Pie Chart)**
   - 展示行为模式的分布比例
   - 支持点击查看详情

3. **时间模式热力图 (Heatmap)**
   - 使用 Canvas API 绘制
   - 展示用户活跃时段分布
   - 支持日期范围选择

#### 5.3.3 转化分析图表

1. **转化漏斗图 (Funnel Chart)**
   - 使用 Canvas API 绘制
   - 展示各阶段的用户数量和转化率
   - 支持阶段展开查看详情

2. **用户轨迹流程图 (Flow Chart)**
   - 使用 Canvas API 绘制
   - 展示用户浏览路径
   - 支持路径筛选和对比

3. **时间线图 (Timeline Chart)**
   - 展示用户行为时间线
   - 支持时间范围选择
   - 支持事件筛选

#### 5.3.4 实时意图识别图表

1. **转化链路可视化**
   - 展示用户当前所处的转化阶段
   - 使用进度条展示转化进度
   - 支持阶段标记

2. **行为统计图表**
   - 柱状图展示不同类型行为的数量
   - 实时更新数据

3. **参与度仪表盘 (Gauge Chart)**
   - 使用 SVG 绘制
   - 展示用户参与度水平
   - 使用颜色编码（低/中/高）

4. **兴趣分布饼图**
   - 展示用户核心兴趣的分布
   - 支持标签筛选

5. **视频交互影响对比图**
   - 对比视频交互前后的转化指标
   - 使用对比柱状图展示
   - 显示提升百分比

### 5.4 交互设计

#### 5.4.1 图表交互

- **悬停效果**：鼠标悬停显示详细数据
- **点击交互**：点击图表元素查看详情
- **缩放功能**：支持图表缩放（部分图表）
- **筛选功能**：支持数据筛选和排序

#### 5.4.2 页面交互

- **标签页切换**：平滑的标签页切换动画
- **卡片展开/收起**：聚类卡片支持展开查看详情
- **搜索功能**：支持聚类名称搜索
- **筛选功能**：支持按特征筛选聚类

#### 5.4.3 实时更新

- **数据刷新**：支持手动刷新数据
- **自动更新**：实时意图识别页面自动更新
- **动画效果**：数据更新时使用动画过渡

### 5.5 UI/UX 设计

#### 5.5.1 设计风格

- **现代化**：使用渐变色彩、卡片式布局
- **专业**：商务风格配色方案
- **简洁**：清晰的信息层次结构

#### 5.5.2 配色方案

- **主色调**：紫色渐变 (#667eea → #764ba2)
- **辅助色**：
  - 成功/积极：绿色 (#00ff88)
  - 警告：黄色 (#ffc107)
  - 错误：红色 (#ff4444)
  - 信息：蓝色 (#7da6ff)
- **中性色**：
  - 背景：浅灰色 (#f8f9fa)
  - 文字：深灰色 (#333)

#### 5.5.3 动画效果

- **页面加载**：淡入动画
- **卡片出现**：从下往上滑入
- **数据更新**：数字递增动画
- **图表渲染**：渐进式渲染

### 5.6 多语言支持

#### 5.6.1 支持语言

- 中文（简体）
- 英文

#### 5.6.2 实现方式

- 使用 `i18n.js` 管理翻译资源
- 使用 `data-i18n` 属性标记需要翻译的元素
- 支持动态内容翻译
- 语言切换使用 localStorage 持久化

---

## 6. 技术架构

### 6.1 系统架构

```
┌─────────────────────────────────────────┐
│          Frontend (可视化层)            │
│  - HTML/CSS/JavaScript                  │
│  - Chart.js / Canvas API                │
│  - i18n.js (多语言支持)                 │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│      Data Processing (数据处理层)       │
│  - Python 脚本                          │
│  - 聚类算法 (K-Means)                  │
│  - 特征工程                             │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│         Data Storage (数据存储层)      │
│  - JSON 文件                            │
│  - CSV 文件                             │
│  - 本地存储 (localStorage)              │
└─────────────────────────────────────────┘
```

### 6.2 数据流

1. **原始数据** → 数据提取脚本 → 结构化数据
2. **结构化数据** → 聚类算法 → 聚类结果
3. **聚类结果** → 用户画像分析 → 画像数据
4. **画像数据** → 业务洞察生成 → 业务建议
5. **所有数据** → 前端可视化 → 用户界面

### 6.3 文件结构

```
意图cluster产品/
├── data_extract/              # 数据提取
│   └── extract_data.py
├── cluster_timeClip/          # 聚类分析
│   ├── behavior_intent_clustering.py
│   ├── business_driven_clustering.py
│   ├── cluster_analysis.py
│   └── visualize_clusters.py
├── user_portrait_analysis/    # 用户画像分析
│   ├── user_portrait_analyzer.py
│   └── business_driven_portrait_analyzer.py
└── visualization_dashboard/   # 前端可视化
    ├── index.html
    ├── styles.css
    ├── dashboard.js
    ├── data.js
    └── i18n.js
```

### 6.4 技术选型

#### 6.4.1 后端技术

- **Python 3.8+**: 主要编程语言
- **pandas**: 数据处理
- **numpy**: 数值计算
- **scikit-learn**: 机器学习（K-Means聚类）
- **json**: 数据序列化

#### 6.4.2 前端技术

- **HTML5**: 页面结构
- **CSS3**: 样式设计
- **JavaScript (ES6+)**: 交互逻辑
- **Chart.js 3.x**: 图表库
- **Canvas API**: 自定义图表绘制

---

## 7. 数据模型

### 7.1 输入数据格式

#### 7.1.1 原始行为数据

```json
{
  "user_id": "user_123",
  "timestamp": "2025-01-01T10:00:00Z",
  "behavior_type": "scroll|hover|click|view",
  "product": "product_name",
  "intent_output": "{...意图识别结果JSON...}"
}
```

#### 7.1.2 意图识别结果

```json
{
  "intent_score": 0.75,
  "core_interests": ["止鼾", "睡眠质量"],
  "product_focus": {
    "category": "Home & Bedding",
    "price_range": "premium",
    "key_attributes": ["Comfort", "Sleep"]
  },
  "purchase_signals": {
    "stage": "considering",
    "concerns": ["功能", "有效性"]
  }
}
```

### 7.2 聚类结果数据格式

#### 7.2.1 聚类元数据

```json
{
  "cluster_id": 0,
  "cluster_name": "高意图·Z6偏好·决策阶段",
  "user_count": 150,
  "segment_count": 200,
  "avg_duration_minutes": 15.5,
  "avg_record_count": 25,
  "avg_intent_score": 0.82
}
```

#### 7.2.2 聚类特征

```json
{
  "cluster_id": 0,
  "features": {
    "purchase_stage": 2,
    "product_preference": 0,
    "price_sensitivity": 2,
    "concern_focus": 1,
    "core_need": 0
  }
}
```

### 7.3 用户画像数据格式

```json
{
  "cluster_id": 0,
  "portrait": {
    "key_characteristics": [
      "购买阶段: 决策阶段",
      "价格偏好: 高端价值型",
      "产品偏好: Z6智能止鼾枕"
    ],
    "behavior_patterns": {
      "high_engagement": 0.8,
      "quick_browse": 0.1,
      "deep_research": 0.7,
      "multi_session": 0.3
    },
    "time_patterns": {
      "active_hours": [20, 21, 22],
      "active_days": ["Monday", "Tuesday"]
    }
  }
}
```

### 7.4 业务洞察数据格式

```json
{
  "cluster_id": 0,
  "insights": {
    "segment_name": "高价值决策用户",
    "key_features": ["高意图强度", "Z6偏好", "决策阶段"],
    "marketing_strategy": "针对性的产品推荐和优惠券",
    "product_recommendation": "Z6智能止鼾枕",
    "conversion_optimization": "简化购买流程，提供快速结账",
    "pricing_strategy": "高端定位，强调价值",
    "content_strategy": "产品对比和用户评价"
  }
}
```

---

## 8. 用户流程

### 8.1 数据导入流程

```
1. 准备原始行为数据
   ↓
2. 运行数据提取脚本
   ↓
3. 运行聚类分析脚本
   ↓
4. 运行用户画像分析脚本
   ↓
5. 生成业务洞察
   ↓
6. 更新前端数据文件 (data.js)
```

### 8.2 前端使用流程

#### 8.2.1 数据总览

```
1. 打开仪表板首页
   ↓
2. 查看数据总览
   ↓
3. 点击图表查看详情
   ↓
4. 切换到其他标签页
```

#### 8.2.2 聚类分析

```
1. 进入"用户分析"标签页
   ↓
2. 浏览聚类列表
   ↓
3. 点击聚类卡片查看详情
   ↓
4. 查看用户画像和业务洞察
   ↓
5. 导出聚类数据
```

#### 8.2.3 实时意图识别

```
1. 打开实时意图识别页面
   ↓
2. 在模拟商店中浏览
   ↓
3. 查看右侧实时意图分析
   ↓
4. 与视频交互
   ↓
5. 查看视频交互对转化的影响
```

### 8.3 数据更新流程

```
1. 收集新的用户行为数据
   ↓
2. 运行聚类分析脚本
   ↓
3. 更新 data.js 文件
   ↓
4. 刷新前端页面查看新数据
```

---

## 9. 性能要求

### 9.1 数据处理性能

- **聚类分析**：支持处理 10,000+ 用户会话数据
- **计算时间**：单次聚类分析 < 30 秒
- **内存占用**：< 2GB

### 9.2 前端性能

- **页面加载**：首屏加载时间 < 3 秒
- **图表渲染**：图表渲染时间 < 1 秒
- **交互响应**：用户操作响应时间 < 100ms
- **数据更新**：实时数据更新延迟 < 500ms

### 9.3 浏览器兼容性

- **Chrome/Edge**: 最新版本（推荐）
- **Firefox**: 最新版本
- **Safari**: 最新版本
- **移动浏览器**: iOS Safari, Chrome Mobile

### 9.4 数据规模

- **聚类数量**：支持 5-30 个聚类
- **用户数量**：支持 1,000-100,000 用户
- **数据点**：支持 10,000-1,000,000 数据点

---

## 10. 未来规划

### 10.1 短期规划 (3个月)

1. **算法优化**
   - ✅ 实现基于意图变化的分段策略（已完成）
   - 实现自动聚类数量优化
   - 添加更多聚类算法选项（DBSCAN, 层次聚类等）
   - 优化特征工程流程
   - 优化意图变化阈值自适应调整

2. **功能增强**
   - 添加聚类对比功能
   - 添加数据导出功能（Excel, PDF）
   - 添加数据筛选和搜索功能

3. **用户体验**
   - 优化移动端体验
   - 添加更多图表类型
   - 改进交互反馈

### 10.2 中期规划 (6个月)

1. **实时分析**
   - 实现实时数据流处理
   - 添加实时聚类更新
   - 实现增量聚类分析

2. **高级分析**
   - 添加用户生命周期分析
   - 添加预测性分析
   - 添加异常检测

3. **集成功能**
   - 与电商平台API集成
   - 与营销工具集成
   - 与CRM系统集成

### 10.3 长期规划 (12个月)

1. **AI增强**
   - 使用深度学习优化聚类
   - 实现自动特征选择
   - 添加自然语言处理能力

2. **平台化**
   - 开发Web API
   - 支持多租户
   - 添加用户权限管理

3. **扩展性**
   - 支持更大规模数据
   - 分布式计算支持
   - 云服务部署

---

## 附录

### A. 术语表

- **聚类 (Cluster)**: 具有相似特征的用户群体
- **意图 (Intent)**: 用户的购买意图和偏好
- **会话 (Session)**: 用户的一次连续浏览行为
- **意图片段 (Intent Segment)**: 基于意图变化分割的用户行为片段，一个用户可以拥有多个意图片段
- **意图变化阈值 (Intent Change Threshold)**: 判断意图是否发生显著变化的阈值（默认0.3，范围0-1）
- **转化阶段 (Conversion Stage)**: 用户在购买流程中的位置

### B. 参考文档

- [聚类算法优化总结](./意图cluster产品/CLUSTERING_OPTIMIZATION_SUMMARY.md)
- [可视化仪表板使用指南](./意图cluster产品/visualization_dashboard/USAGE_GUIDE.md)
- [用户画像分析说明](./意图cluster产品/user_portrait_analysis/README.md)

### C. 联系方式

- **技术支持**: support@example.com
- **产品反馈**: feedback@example.com

---

**文档版本历史**

| 版本 | 日期 | 作者 | 说明 |
|------|------|------|------|
| v1.1 | 2025-01-XX | Product Team | 更新：实现基于意图变化的分段策略 |
| v1.0 | 2025-01-XX | Product Team | 初始版本 |

